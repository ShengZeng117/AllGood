const Manager = require('../models/Manager')
const Device = require('../models/Devices')

/*const logIn = (req, res) => {
    res.render('staff_login.hbs', { layout: 'staff_loginD', message: req.flash('message') })
}*/

module.exports = {}